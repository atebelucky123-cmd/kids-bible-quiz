import { Router } from "express";

// Endpoints (start, get, answer, complete, result) are implemented across
// Phase 7 — Quiz Engine, Phase 8 — Answering & Persistence, and
// Phase 9 — Scoring & Results.
export const quizRouter = Router();
