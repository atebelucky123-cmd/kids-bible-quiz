import { Router } from "express";

// Endpoints (question CRUD, users, attempts) are implemented in
// Phase 11 — Admin Dashboard, protected by admin authorization middleware
// added in that phase.
export const adminRouter = Router();
